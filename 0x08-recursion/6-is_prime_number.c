#include "main.h"

/**
 * is_prime_number - returns i if is an prime number
 *
 * @n: parameter
 * Return: returns 1 if its not a prime number
 * Otherwise 0
 */
int is_prime_number(int n)
{
	if (n < = 1)
		return (0);
	else
		return (is_prime_
