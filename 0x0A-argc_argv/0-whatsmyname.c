#include <stdio.h>

/**
 * main - entry point
 * @argv: array of commandlind arguments
 * @argc: number of the arguments
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
